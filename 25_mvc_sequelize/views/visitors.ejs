<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>visitors</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <link rel="stylesheet" href="/static/css/visitor.css">
</head>

<body>
    <form method="post" name="visitor-form">
        <fieldset>
            <legend>방명록 등록</legend>
            <input type="text" name="name" id="name" placeholder="이름 (10글자 이하)" required><br>
            <input type="text" name="comment" id="comment" placeholder="방명록" required><br>
            <div class="button-group">
                <button type="button" onclick="createVisitor();">등록</button>
            </div>
        </fieldset>
    </form>

    <table border="1" cellspacing="0">
        <thead>
            <tr>
                <td>ID</td>
                <td>작성자</td>
                <td>방명록</td>
                <td>수정</td>
                <td>삭제</td>
            </tr>
        </thead>
        <tbody>
            <% for (i=0; i < data.length; i++){ %> 
            <tr id="tr_<%=data[i].id%>">
                <td><%=data[i].id%></td>
                <td><%=data[i].name%></td>
                <td><%=data[i].comment%></td>
                <td><button type="button" onclick="editVisitor('<%=data[i].id%>');">수정</button></td>
                <td><button type="button" onclick="deleteVisitor(this, '<%=data[i].id%>');">삭제</button></td>
            </tr>    
            <%}%>
        </tbody>
    </table>
    <script src="/static/js/visitor.js"></script>
</body>
</html>